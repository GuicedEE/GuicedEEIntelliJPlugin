package ${PACKAGE_NAME};

import com.guicedee.client.services.lifecycle.IOnCallScopeExit;

/**
 * A hook that runs when a call scope is exited.
 */
public class ${NAME} implements IOnCallScopeExit<${NAME}> {

    @Override
    public void onScopeExit() {
        // Add your logic here
    }
    
    @Override
    public Integer sortOrder() {
        return 100; // Lower numbers run first
    }
}