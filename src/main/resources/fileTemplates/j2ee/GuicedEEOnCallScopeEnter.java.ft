package ${PACKAGE_NAME};

import com.guicedee.client.services.lifecycle.IOnCallScopeEnter;
import com.google.inject.Scope;

/**
 * A hook that runs when a call scope is entered.
 */
public class ${NAME} implements IOnCallScopeEnter<${NAME}> {

    @Override
    public void onScopeEnter(Scope scope) {
        // Add your logic here
    }
    
    @Override
    public Integer sortOrder() {
        return 100; // Lower numbers run first
    }
}