package ${PACKAGE_NAME};

import com.guicedee.guicedservlets.websockets.options.WebSocketMessageReceiver;
import com.guicedee.guicedservlets.websockets.services.IWebSocketMessageReceiver;
import lombok.extern.slf4j.Slf4j;

import java.util.HashSet;
import java.util.Set;

@Slf4j
public class ${NAME} implements IWebSocketMessageReceiver
{
    @Override
    public io.smallrye.mutiny.Uni<Void> receiveMessage(com.guicedee.guicedservlets.websockets.options.WebSocketMessageReceiver<?> message) throws SecurityException {
        return io.smallrye.mutiny.Uni.createFrom().voidItem();
    }

    @Override
    public Set<String> messageNames()
    {
        Set<String> messageNames = new HashSet<>();
        messageNames.add("ActionType");
        return messageNames;
    }
}
